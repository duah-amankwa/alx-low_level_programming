#include <stdio.h>
/**
 * main- Entry point
 *
 * Return : Always 0 
 */
int main (void)
{
	int singleDigit;

	for (singleDigit = 0; singleDigit < 10; singleDigit++)
		printf("%d",singleDigit);
		printf("\n");
		
	return (0);
}
